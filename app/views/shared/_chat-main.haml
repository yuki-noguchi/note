%li.message.clearfix{ data:{ id: message.id } }
  .message__avatar
    = user_avatar(message.talker)
  .message__nickname
    = user_nickname(message.talker)
  .message__time
    = message.created_at.strftime('%Y/%m/%d %H:%M')
  .message__body
    = message.body
